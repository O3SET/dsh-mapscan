# 安全策略

## 报告漏洞

如果你发现安全漏洞，请**不要**公开提交 Issue。请通过私有渠道（如 GitHub Security Advisory 或直接联系维护者）报告，我们将在确认后尽快修复并发布安全公告。

## 支持范围

| 版本     | 支持        |
| -------- | ----------- |
| 最新版本 | ✅ 安全更新 |

## 密钥安全约定

- **绝不**把 API Key 写入源码、测试、示例或提交到仓库
- 本插件的 Key 仅通过以下方式注入：
  1. `map_set_keys` 写入 DSH 凭证库（不落盘到仓库）
  2. 环境变量（`MAPSCAN_*_API_KEY` 或平台惯用名）
  3. 工具调用参数 `key`（一次性，不持久化）
- 如怀疑 Key 泄露，请立即到对应平台个人中心重置，并调用 `map_set_keys { "remove": ["<平台>"] }` 清除本地凭证

## 授权使用声明

本工具为安全研究/授权测试辅助工具。使用者须遵守目标平台服务条款与当地法律法规；本项目对滥用行为不承担责任。
